package com.example.junit_test

class Calculator {
    fun add(first: Int, second: Int): Int {
        return second + first
    }
    fun sub(first: Int, second: Int): Int {
            return first - second
    }
}
